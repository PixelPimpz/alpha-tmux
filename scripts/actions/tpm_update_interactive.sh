#!/usr/bin/env bash
cat << EOF 
"this will provide a pop-up menu list of plugins installed with TPM
and allow the user to choose which plugin(s) to update. Ideally, 
it should run the update code and automatically reload the tmux config"
EOF
