#!/usr/bin/env bash
 SCRIPT_PATH="$( readlink -f "${BASH_SOURCE[0]:-$0}" )"
 PLUGIN_ROOT="$( cd "$( dirname "$SCRIPT_PATH" )/.." && pwd )"
 source "$PLUGIN_ROOT/scripts/utils/nerds.sh"
add-glyph "$@"
